export const darkMode = true;
export const defaultMainView = 'grid';
export const defaultSummariesView = 'visualizations';

const RELEASE = false;

export const apiBaseUrl = RELEASE ? './' : 'http://localhost:8080/';
